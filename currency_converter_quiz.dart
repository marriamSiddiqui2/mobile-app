import 'package:flutter/material.dart';

class ConverterCurrency extends StatelessWidget {
  const ConverterCurrency({super.key});

  @override
  Widget build(BuildContext context) {
    return const
      Text( '0',
            style: TextStyle(
              fontSize: 30,
              color: Color.fromARGB(255, 188, 90, 90),
            ),
    );
  }
}